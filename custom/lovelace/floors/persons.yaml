# Persons *******************************************

- entities: 
    - person.benjamin_selwyn
    - person.clare_selwyn
    - person.roger_selwyn
    - device_tracker.pet_brie
    - device_tracker.pet_stilton
  state_action:
    - action: call-service
      service: floorplan.class_set
      service_data: 'person person-${entity.state}'

- entities: 
    - input_text.people_at_home
  state_action:
    - action: call-service
      service: floorplan.text_set
      service_data: |
        > return `${entity.state}`
        
    # - action: call-service
    #   service: floorplan.class_set
    #   service_data: |
    #     > 
    #     return `climate climate-${entity.attributes.hvac_action}`;